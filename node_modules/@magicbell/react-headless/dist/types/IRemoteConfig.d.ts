export declare type WebPushConfig = {
    subscribeUrl: string;
    vapidAuthentication: {
        publicKey: string;
    };
};
export interface Channel<T> {
    enabled: boolean;
    config: T;
}
export declare type Channels = {
    webPush: Channel<WebPushConfig>;
};
export declare type InboxConfig = {
    features: any;
};
export declare type WebSocketConfig = {
    region: string;
    channel: string;
    authUrl: string;
};
export default interface IRemoteConfig {
    channels?: Channels;
    inbox?: InboxConfig;
    ws?: WebSocketConfig;
}
