/// <reference types="react" />
declare type TextProps = {
    id: string;
    defaultMessage: string;
    html?: boolean;
};
/**
 *
 * @example
 * <Text id="header.title" defaultMessage="My Notifications" />
 */
export default function Text({ id, defaultMessage, html }: TextProps): JSX.Element;
export {};
