/** @jsx jsx */
import { jsx } from '@emotion/react';
interface Props {
    attrs?: any;
    children: any;
    width?: number;
    layout: string[];
}
/**
 * Styled container to wrap the notification inbox with.
 *
 * @example
 * <StyledPopoverContainer>
 *   <Notifications />
 * </StyledPopoverContainer>
 */
export default function StyledPopoverContainer({ width, children, attrs, layout }: Props): jsx.JSX.Element;
export {};
